# ★.ps1 必須是 ASCII-only 且★不加 BOM★(2026-08-03 Windows 真機回歸測試裁定)★
#
# 前一版曾規定「必須加 BOM」——★那條已被推翻★:BOM 是兩難不是解法。
#   無 BOM + 非 ASCII → 磁碟執行時 PowerShell 5.1 用系統 ANSI codepage 讀,
#                       DBCS 前導位元組吃掉下一個位元組、撞壞引號配對(CJK 語系 parse error)
#   有 BOM            → `irm | iex` 收到的是普通字串,PowerShell 不會替它剝 BOM,
#                       U+FEFF 成了第 1 行內容、那行註解被當成指令執行
#                       (真 PowerShell 實測:The term 'Windows/PowerShell' is not recognized)
# 正解=ASCII-only:兩條路徑都不再需要 BOM。中文設計說明放 slim/WINDOWS-NOTES.md。
#
# 因此這裡★不宣告 working-tree-encoding★(那是給需要 BOM 的檔用的);
# 只保留 eol=CRLF 對齊 Windows 慣例。ASCII-only 由測試 t_slim_ps1_ascii_only_no_bom 守。
*.ps1 text eol=CRLF
